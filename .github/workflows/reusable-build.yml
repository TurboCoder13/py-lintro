---
# yamllint disable rule:line-length
# Reusable workflow: Build Python artifacts and validate metadata
# - Sets up Python and uv
# - Syncs dev dependencies
# - Builds sdist and wheel
# - Runs twine check to validate artifacts
# - Uploads dist/ as an artifact named "dist"
name: Reusable Build

'on':
  workflow_call:
    inputs:
      python-version:
        description: Python version to use
        type: string
        required: false
        default: '3.13'

jobs:
  build:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    permissions:
      contents: read
    steps:
      - name: Harden and Checkout
        uses: TurboCoder13/py-lintro/.github/actions/harden-and-checkout@f5c9374c8f60487ac34781fb6fb78496be0699bc
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          egress_policy: block
          allowed-endpoints: |
            ${{ vars.EGRESS_ALLOWED_ENDPOINTS }}
            ${{ vars.EGRESS_ALLOWED_ENDPOINTS_PYPI }}
      - name: Ensure GITHUB_TOKEN is set for uv install
        run: echo "GITHUB_TOKEN=${{ secrets.GITHUB_TOKEN }}" >> "$GITHUB_ENV"

      - name: Environment setup
        uses: TurboCoder13/py-lintro/.github/actions/setup-env@f5c9374c8f60487ac34781fb6fb78496be0699bc
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          python-version: ${{ inputs.python-version }}

      - name: Sync dependencies
        run: uv sync --dev --no-progress

      - name: Build sdist and wheel
        run: uv run python -m build

      - name: Twine check
        run: uv run python -m twine check dist/*

      - name: Upload dist artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: dist
          path: dist/
