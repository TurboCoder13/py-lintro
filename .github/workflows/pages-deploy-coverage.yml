---
name: GitHub Pages
'on':
  workflow_run:
    workflows: [Test Suite & Coverage, Test and Coverage]
    types:
      - completed
    branches:
      - main
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: pages
  cancel-in-progress: false
jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    defaults:
      run:
        shell: bash
    if: >-
      ${{ github.event.workflow_run.conclusion == 'success' ||
          github.event_name == 'workflow_dispatch' }}
    steps:
      - name: Harden and Checkout
        uses: TurboCoder13/py-lintro/.github/actions/harden-and-checkout@f73ba3032c8e1f25f72c033f8a8b590eec23257a
      - name: Download coverage artifact
        uses: dawidd6/action-download-artifact@ac66b43f0e6a346234dd65d4d0c8fbb31cb316e5 # v11
        with:
          workflow: test-and-coverage.yml
          name: coverage-report-python-3.13
          path: coverage-report
          workflow_conclusion: success
      - name: Download coverage badge artifact
        uses: dawidd6/action-download-artifact@ac66b43f0e6a346234dd65d4d0c8fbb31cb316e5 # v11
        with:
          workflow: test-and-coverage.yml
          name: coverage-badge
          path: coverage-badge
          workflow_conclusion: success
      - name: Create coverage report directory
        run: ./scripts/ci/pages-deploy.sh --badge-path coverage-badge/coverage-badge.svg
      - name: Deploy coverage to Pages
        id: deployment
        uses: TurboCoder13/py-lintro/.github/actions/pages-deploy@f73ba3032c8e1f25f72c033f8a8b590eec23257a
        with:
          path: _site
